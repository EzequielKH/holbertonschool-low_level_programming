#include "main.h"

/**
 * main - check the code
 *
 * Return: Always 0 (success).
 */
int main(void)
{
	print_triangle(2);   /* Triangle of size 2 */
	print_triangle(10);  /* Triangle of size 10 */
	print_triangle(1);   /* Triangle of size 1 */
	print_triangle(0);   /* No triangle, just a newline */

	return (0);
}
